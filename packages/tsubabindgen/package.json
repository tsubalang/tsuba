{
  "name": "@tsuba/tsubabindgen",
  "version": "0.0.1",
  "description": "Generate Tsuba bindings facades for Rust crates",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "tsubabindgen": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "pretest": "npm run build",
    "test": "mocha --parallel 'dist/**/*.test.js'"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
